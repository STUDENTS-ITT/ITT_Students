# Отчёт по фильтру Калмана бортовой инерциальной навигационной системы (БИНС)

Полное описание фильтра Калмана, используемого в программе БИНС: определения, формулы, логика работы, настройка параметров.

---

## Содержание

1. [Общее описание](#1-общее-описание)
2. [Архитектура системы](#2-архитектура-системы)
3. [Вектор состояний](#3-вектор-состояний)
4. [Вектор измерений](#4-вектор-измерений)
5. [Матрица перехода F](#5-матрица-перехода-f)
6. [Матрица шума процесса Q](#6-матрица-шума-процесса-q)
7. [Матрица наблюдения H](#7-матрица-наблюдения-h)
8. [Матрица шума измерений R](#8-матрица-шума-измерений-r)
9. [Ковариационная матрица P](#9-ковариационная-матрица-p)
10. [Коэффициент усиления Калмана K](#10-коэффициент-усиления-калмана-k)
11. [Уравнения фильтра](#11-уравнения-фильтра)
12. [Интеграция с БИНС](#12-интеграция-с-бинс)
13. [Числовые примеры](#13-числовые-примеры)
14. [Руководство по настройке](#14-руководство-по-настройке)

---

## 1. Общее описание

Фильтр Калмана 15-го порядка, угловатый (error-state). Оценивает ошибки навигационных параметров БИНС по расхождению с данными СНС (GPS + angle.dat).

**Тип фильтра:** Error-state Kalman Filter (ESKF)

**Принцип работы:**
- БИНС интегрирует показания ИМУ (200 Гц) → получает координаты, скорости, углы
- Фильтр Калмана оценивает **ошибки** этих величин по расхождению с СНС
- Ошибки применяются к состоянию БИНС → результат уточняется

**Частоты:**
- Интегрирование: 200 Гц (каждый такт ИМУ)
- Коррекция: ~10 Гц (при обновлении GPS)

---

## 2. Архитектура системы

```
ИМУ (200 Гц)                    GPS (10 Гц)              angle.dat (10 Гц)
    │                               │                          │
    ▼                               ▼                          ▼
┌─────────┐                   ┌──────────┐              ┌──────────┐
│ БИНС    │                   │ SnsReader│              │ (опциональный) │
│ Интегр. │                   │ GPS+angle│              │          │
└────┬────┘                   └────┬─────┘              └────┬─────┘
     │                             │                          │
     │    ┌────────────────────────┴──────────────────────────┘
     │    │
     ▼    ▼
┌─────────────────┐
│  Фильтр Калмана │
│  15-го порядка  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Результат      │
│  result.txt     │
└─────────────────┘
```

**Входные данные:**
- `imu.dat`: time_s, timestamp_ns, wx, wy, wz, ax, ay, az (200 Гц)
- `gps.dat`: time_s, timestamp_ns, lat, lon, alt, vx, vy, vz (10 Гц)
- `angle.dat` (опциональный): time_s, timestamp_ns, roll, pitch, yaw (10 Гц)

**Выходные данные:**
- `result.txt`: time, lon, lat, alt, heading, pitch, roll, vn, vh, ve (200 Гц)
- `reference.txt`: time, lon, lat, alt, heading, pitch, roll, vn, vh, ve (~10 Гц)
- `errors.txt`: time, x0..x14 (~10 Гц)

---

## 3. Вектор состояний

### 3.1. Навигационное состояние (NavState)

Полное состояние навигации, интегрированное по ИМУ:

| Компонента | Обозначение | Единицы | Описание |
|------------|-------------|---------|----------|
| lat | φ | рад | Широта |
| lon | λ | рад | Долгота |
| alt | h | м | Высота |
| V[0] | Vn | м/с | Скорость на север |
| V[1] | Vh | м/с | Вертикальная скорость |
| V[2] | Ve | м/с | Скорость на восток |
| att.heading | ψ | рад | Курс |
| att.pitch | θ | рад | Тангаж |
| att.roll | φ | рад | Крен |
| ba[0..2] | ba | м/с² | Смещение акселерометра (3 оси) |
| bg[0..2] | bg | рад/с | Смещение гироскопа (3 оси) |

### 3.2. Вектор ошибок фильтра Калмана x (15×1)

Это **вектор ошибок** (не само состояние). Каждый элемент — разность между истинным и оценённым значением:

| Индекс | Обозначение | Физический смысл | Единицы |
|--------|-------------|-------------------|---------|
| x[0] | δφ | Ошибка широты | рад |
| x[1] | δλ | Ошибка долготы | рад |
| x[2] | δh | Ошибка высоты | м |
| x[3] | δVn | Ошибка скорости на север | м/с |
| x[4] | δVh | Ошибка вертикальной скорости | м/с |
| x[5] | δVe | Ошибка скорости на восток | м/с |
| x[6] | δψ | Ошибка курса | рад |
| x[7] | δθ | Ошибка тангажа | рад |
| x[8] | δφ | Ошибка крена | рад |
| x[9] | δba_x | Ошибка смещения акселерометра X | м/с² |
| x[10] | δba_y | Ошибка смещения акселерометра Y | м/с² |
| x[11] | δba_z | Ошибка смещения акселерометра Z | м/с² |
| x[12] | δbg_x | Ошибка смещения гироскопа X | рад/с |
| x[13] | δbg_y | Ошибка смещения гироскопа Y | рад/с |
| x[14] | δbg_z | Ошибка смещения гироскопа Z | рад/с |

**Связь с навигационным состоянием:**

```
истинное = оценённое − ошибка     (координаты, скорости, углы)
истинное = оценённое + ошибка     (смещения датчиков)
```

После коррекции x обнуляется — все ошибки «применены» к состоянию.

### 3.3. Производные (для метода трапеций)

| Компонента | Обозначение | Описание |
|------------|-------------|----------|
| V_dot_prev | V̇ | Производная скорости на предыдущем такте |
| lat_dot_prev | φ̇ | Производная широты на предыдущем такте |
| lon_dot_prev | λ̇ | Производная долготы на предыдущем такте |
| alt_dot_prev | ḣ | Производная высоты на предыдущем такте |
| rates_prev | ω̇ | Производные углов ориентации на предыдущем такте |

---

## 4. Вектор измерений

### 4.1. Измерения БИНС (bins)

Текущее решение БИНС для вычисления инновации:

```
bins = {lat, lon, alt, Vn, Vh, Ve, ψ, θ, φ}
```

### 4.2. Измерения СНС (sns)

Эталон из gps.dat + angle.dat:

```
sns = {lat_gps, lon_gps, alt_gps, Vn_gps, Vh_gps, Ve_gps, ψ_gps, θ, φ}
```

**Важно:** тангаж и крен берутся из текущего состояния БИНС (инновация = 0 → коррекции нет). Курс берётся из angle.dat (если есть).

### 4.3. Вектор инновации z (9×1)

Разность между решением БИНС и эталоном СНС:

```
z = bins − sns
```

| Компонента | Обозначение | Физический смысл |
|------------|-------------|-------------------|
| z[0] | δφ_bins − δφ_gps | Ошибка широты БИНС относительно GPS |
| z[1] | δλ_bins − δλ_gps | Ошибка долготы БИНС относительно GPS |
| z[2] | δh_bins − δh_gps | Ошибка высоты БИНС относительно GPS |
| z[3] | δVn_bins − δVn_gps | Ошибка скорости N БИНС относительно GPS |
| z[4] | δVh_bins − δVh_gps | Ошибка вертикальной скорости БИНС |
| z[5] | δVe_bins − δVe_gps | Ошибка скорости E БИНС относительно GPS |
| z[6] | δψ_bins − δψ_gps | Ошибка курса БИНС относительно angle.dat |
| z[7] | 0 | Тангаж: инновация обнулена |
| z[8] | 0 | Крен: инновация обнулена |

---

## 5. Матрица перехода F

### 5.1. Общая структура

Матрица 15×15 описывает, как ошибки **эволюционируют** за один шаг T:

```
F = I + F_c · T
```

Где I — единичная матрица, F_c — непрерывная модель ошибок, T — шаг дискретизации (0.005 с).

### 5.2. Блок 1: Координаты ← Скорости (строки 0-2, столбцы 3-5)

| Элемент | Формула | Физический смысл |
|---------|---------|-------------------|
| F[0,3] | T / R | δφ̇ = δVn / R |
| F[1,5] | T / (R·cosφ) | δλ̇ = δVe / (R·cosφ) |
| F[2,4] | T | δḣ = δVh |

Физика: если скорость на север завышена на 1 м/с, то за T секунд координата ошибается на T/R радиан.

Защита от сингулярности: `cos(lat)` → `fmax(fabs(cos(lat)), 1e-10)`.

### 5.3. Блок 2: Скорости ← Координаты (строка 4, столбец 2)

| Элемент | Формула | Физический смысл |
|---------|---------|-------------------|
| F[4,2] | T · 2g/R | Ошибка высоты → ошибка вертикальной скорости (гравитационный градиент) |

Физика: на высоте h ускорение свободного падения уменьшается на ~2g·h/R.

### 5.4. Блок 3: Скорости ← Ориентация (строки 3-5, столбцы 6-8)

| Элемент | Формула | Физический смысл |
|---------|---------|-------------------|
| F[3+r, 6] | T · (n_psi × f_nav)[r] | Ошибка курса → ошибка скорости |
| F[3+r, 7] | T · (n_theta × f_nav)[r] | Ошибка тангажа → ошибка скорости |
| F[3+r, 8] | T · (n_gamma × f_nav)[r] | Ошибка крена → ошибка скорости |

Где:
- `n_psi = {0, 1, 0}` — ось вращения курса
- `n_theta = {sin(ψ), 0, cos(ψ)}` — ось вращения тангажа
- `n_gamma = C[:,0]` — ось вращения крена (продольная ось тела)

Физика: если ориентация ошибочна, акселерометр «видит» перегрузку в неверном направлении → ошибка ускорения → ошибка скорости.

### 5.5. Блок 4: Скорости ← Смещения акселерометра (строки 3-5, столбцы 9-11)

| Элемент | Формула | Физический смысл |
|---------|---------|-------------------|
| F[3, 9+j] | T · C[0,j] | Смещение акселя по оси j → ошибка скорости на север |
| F[4, 9+j] | T · C[1,j] | Смещение акселя по оси j → ошибка вертикальной скорости |
| F[5, 9+j] | T · C[2,j] | Смещение акселя по оси j → ошибка скорости на восток |

Физика: смещение акселерометра проецируется в навигационную СК через матрицу C.

### 5.6. Блок 5: Ориентация ← Смещения гироскопа (строки 6-8, столбцы 12-14)

| Элемент | Формула | Физический смысл |
|---------|---------|-------------------|
| F[6, 12..14] | T · e_rate[0][0..2] | Смещение гиро → ошибка курса |
| F[7, 12..14] | T · e_rate[1][0..2] | Смещение гиро → ошибка тангажа |
| F[8, 12..14] | T · e_rate[2][0..2] | Смещение гиро → ошибка крена |

Связывающая матрица Эйлера:

```
e_rate = {{0,       cos(φ)/cos(θ),  -sin(φ)/cos(θ)},
          {0,       sin(φ),          cos(φ)},
          {1,       -tan(θ)cos(φ),   tan(θ)sin(φ)}}
```

Защита от сингулярности при θ = ±90°: `cos(θ)` → `fmax(fabs(cos(θ)), 1e-10)`.

---

## 6. Матрица шума процесса Q

### 6.1. Определение

Диагональная матрица 15×15. Описывает **неопределённость модели** за один шаг T.

### 6.2. Параметры шума

| Параметр | Значение | Единицы | Описание |
|----------|----------|---------|----------|
| σ_g | 3.394e-4 | рад/√с | Шум гироскопа |
| σ_a | 3.05e-3 | м/с²/√с | Шум акселерометра |
| σ_bg | 1.16e-5 | рад/с/√с | Дрейф смещения гироскопа |
| σ_ba | 2e-5 | м/с²/√с | Дрейф смещения акселерометра |

### 6.3. Формулы элементов Q

```
Q[0..2, 0..2] = 0                          (координаты — без шума)
Q[3..5, 3..5] = σ_a² · T = 4.65e-8        (скорости)
Q[6..8, 6..8] = σ_g² · T = 5.75e-10       (углы)
Q[9..11, 9..11] = σ_ba² · T = 2e-12       (смещения акселя)
Q[12..14, 12..14] = σ_bg² · T = 6.73e-13  (смещения гиро)
```

### 6.4. Физический смысл

За каждый шаг T=0.005 с:
- Неопределённость скорости вырастает на ~4.65e-8 (м/с)² из-за шума акселерометра
- Неопределённость углов вырастает на ~5.75e-10 рад² из-за шума гироскопа

За 1 секунду (200 шагов):
- σ_V вырастает на ~0.003 м/с
- σ_углов вырастает на ~0.001°

---

## 7. Матрица наблюдения H

### 7.1. Определение

Матрица 9×15. Определяет, **какие компоненты состояния** мы измеряем напрямую.

### 7.2. Структура

```
H = [ I₉  |  0₉×6 ]
```

### 7.3. Элементы

| Строка | Столбец | H[j,j] | Что измеряем |
|--------|---------|--------|--------------|
| 0 | 0 | 1 | Широта БИНС = широта СНС |
| 1 | 1 | 1 | Долгота БИНС = долгота СНС |
| 2 | 2 | 1 | Высота БИНС = высота СНС |
| 3 | 3 | 1 | Скорость N БИНС = скорость N СНС |
| 4 | 4 | 1 | Скорость H БИНС = скорость H СНС |
| 5 | 5 | 1 | Скорость E БИНС = скорость E СНС |
| 6 | 6 | 1 | Курс БИНС = курс СНС |
| 7 | 7 | 1 | Тангаж БИНС = тангаж СНС |
| 8 | 8 | 1 | Крен БИНС = крен СНС |

Столбцы 9-14 = 0: смещения акселя/гиро **ненаблюдаемы** напрямую. Они оцениваются косвенно.

### 7.4. Особенности реализации

Строки 7,8 (тангаж/крен) фактически обнулены (инновация = 0), т.к. `sns[7] = st.att.pitch`, `sns[8] = st.att.roll`.

---

## 8. Матрица шума измерений R

### 8.1. Определение

Диагональная матрица 9×9. Описывает **точность GPS и angle.dat**.

### 8.2. Параметры точности

| Параметр | Значение | Единицы | Описание |
|----------|----------|---------|----------|
| σ_pos | 5.0 / R_earth ≈ 7.85e-7 | рад | Точность GPS по координатам (~5 м) |
| σ_h | 5.0 | м | Точность GPS по высоте (~5 м) |
| σ_v | 0.1 | м/с | Точность GPS по скорости (~0.1 м/с) |
| σ_ang | 1.0 × π/180 ≈ 0.0175 | рад | Точность angle.dat (~1°) |

### 8.3. Формулы элементов

```
R[0,0] = σ_pos² = (5/R)² ≈ 6.14e-9   (широта)
R[1,1] = σ_pos² = (5/R)² ≈ 6.14e-9   (долгота)
R[2,2] = σ_h² = 25                     (высота)
R[3,3] = σ_v² = 0.01                   (скорость N)
R[4,4] = σ_v² = 0.01                   (скорость H)
R[5,5] = σ_v² = 0.01                   (скорость E)
R[6,6] = σ_ang² ≈ 3.05e-4             (курс)
R[7,7] = σ_ang² ≈ 3.05e-4             (тангаж)
R[8,8] = σ_ang² ≈ 3.05e-4             (крен)
```

### 8.4. Физический смысл

Чем больше R → тем меньше доверия к измерению → тем слабее коррекция.

R[0,0] = 6.14e-9 рад² ≈ (5 м)² / R² — это «перевод» погрешности 5 метров в угловые единицы.

---

## 9. Ковариационная матрица P

### 9.1. Определение

Матрица 15×15. Описывает **неопределённость** текущей оценки ошибок.

### 9.2. Начальное значение P₀

Диагональная матрица. Каждый элемент — квадрат неопределённости начальной оценки.

| Элемент | Значение | Формула | Неопределённость |
|---------|----------|---------|------------------|
| P[0,0] | 6.14e-9 | (50м / R)² | ~50 м |
| P[1,1] | 6.14e-9 | (50м / R)² | ~50 м |
| P[2,2] | 100 | (10м)² | ~10 м |
| P[3,3] | 6.25 | (2.5 м/с)² | ~2.5 м/с |
| P[4,4] | 6.25 | (2.5 м/с)² | ~2.5 м/с |
| P[5,5] | 6.25 | (2.5 м/с)² | ~2.5 м/с |
| P[6,6] | 6.85e-2 | (15° × π/180)² | ~15° |
| P[7,7] | 3.05e-6 | (0.1° × π/180)² | ~0.1° |
| P[8,8] | 3.05e-6 | (0.1° × π/180)² | ~0.1° |
| P[9..11] | 9e-6 | (0.003 м/с²)² | ~0.003 м/с² |
| P[12..14] | 1e-8 | (1e-4 рад/с)² | ~0.006°/с |

### 9.3. Эволюция P

**Предсказание:** `P = F·P·F^T + Q`
- F·P·F^T: ошибки «распространяются» через модель динамики
- Q: добавляется неопределённость от шума датчиков

**Коррекция:** `P = (I - K·H)·P`
- K·H: «сжатие» неопределённости по измеренным компонентам
- Неизмеренные компоненты (ba, bg) уменьшаются косвенно

---

## 10. Коэффициент усиления Калмана K

### 10.1. Формула

```
K = P · H^T · S⁻¹
```

Где `S = H · P · H^T + R` — ковариация инновации.

### 10.2. Физический смысл

K[i,j] — насколько сильно инновация по j-й компоненте измерения влияет на i-ю компоненту состояния.

| Блок K | Физический смысл |
|--------|-------------------|
| K[0..2, 0..2] | Инновация по координатам → коррекция координат |
| K[3..5, 3..5] | Инновация по скоростям → коррекция скоростей |
| K[6..8, 6..8] | Инновация по углам → коррекция углов |
| K[0..2, 3..5] | Инновация по скоростям → коррекция координат |
| K[3..5, 0..2] | Инновация по координатам → коррекция скоростей |
| K[3..5, 6..8] | Инновация по углам → коррекция скоростей |
| K[6..8, 12..14] | Инновация по углам → оценка смещения гиро |
| K[9..14, *] | Любая инновация → оценка смещений (косвенная) |

### 10.3. Зависимость K от P и R

| Условие | K | Поведение |
|---------|---|-----------|
| P большая, R малая | K → H^T | Полная коррекция |
| P малая, R большая | K → 0 | Коррекция минимальна |
| P большая, R большая | K средний | Баланс |

---

## 11. Уравнения фильтра

### 11.1. Предсказание (на каждом такте ИМУ, 200 Гц)

```
x = F · x
P = F · P · F^T + Q
```

### 11.2. Коррекция (при обновлении GPS, ~10 Гц)

```
z = БИНС − СНС                    (инновация)
S = H · P · H^T + R               (ковариация инновации)
K = P · H^T · S⁻¹                 (коэффициент усиления)
x = x + K · (z − H · x)           (коррекция состояния)
P = (I − K · H) · P               (коррекция ковариации)
```

### 11.3. Применение коррекции к состоянию

```
lat  -= x[0]    lon  -= x[1]    alt  -= x[2]
Vn   -= x[3]    Vh   -= x[4]    Ve   -= x[5]
ψ    -= x[6]    θ    -= x[7]    φ    -= x[8]
ba   += x[9..11]
bg   += x[12..14]
x = 0                                    (обнуление)
```

---

## 12. Интеграция с БИНС

### 12.1. Алгоритм на каждом такте ИМУ

```
1. Чтение imu.dat → row
2. Синхронизация gps.dat по времени
3. Интегрирование БИНС:
   a. Матрица направляющих косинусов: C = bodyToNavMatrix(ψ, θ, φ)
   b. Ускорение в навигационной СК: f_nav = C · (f_body − ba)
   c. Предсказание фильтра: x = F·x, P = F·P·F^T + Q
   d. Вредные ускорения: a_harm = Coriolis + centripetal + gravity
   e. Интегрирование скоростей: V = ∫(f_nav − a_harm)dt
   f. Интегрирование координат: (lat, lon, alt) = ∫V dt
   g. Интегрирование углов: (ψ, θ, φ) = ∫ω dt
4. Если gps обновился:
   a. Формирование инновации: z = bins − sns
   b. Вычисление K
   c. Коррекция x и P
   d. Применение ошибок к состоянию
   e. Обнуление x
5. Запись результата в result.txt (200 Гц)
6. Запись эталона в reference.txt (при коррекции)
7. Запись ошибок в errors.txt (при коррекции)
```

### 12.2. Матрица перехода от СК тела к навигационной СК

```
C = bodyToNavMatrix(ψ, θ, φ)

C = {{cos(θ)cos(ψ),  -cos(φ)cos(ψ)sin(θ) + sin(φ)sin(ψ),  sin(φ)cos(ψ)sin(θ) + cos(φ)sin(ψ)},
     {sin(θ),         cos(φ)cos(θ),                          -sin(φ)cos(θ)},
     {-cos(θ)sin(ψ),  cos(φ)sin(ψ)sin(θ) + sin(φ)cos(ψ),  -sin(φ)sin(ψ)sin(θ) + cos(φ)cos(ψ)}}
```

### 12.3. Интегрирование методом трапеций

```
V[n] = V[n-1] + (V̇[n] + V̇[n-1])/2 · T
lat[n] = lat[n-1] + (φ̇[n] + φ̇[n-1])/2 · T
```

---

## 13. Числовые примеры

### 13.1. Коррекция скорости

Допустим, инновация по скорости на север: δVn = 0.5 м/с.

- P[3,3] = 6.25 (неуверенность ~2.5 м/с)
- R[3,3] = 0.01 (GPS точен до 0.1 м/с)
- K[3,3] ≈ P[3,3] / (P[3,3] + R[3,3]) ≈ 0.998

Фильтр доверяет GPS на 99.8%: x[3] += 0.998 · 0.5 ≈ 0.499 м/с.

### 13.2. Коррекция курса (с angle.dat)

Допустим, инновация по курсу: δψ = 0.1 рад (~5.7°).

- P[6,6] = 0.0685 (неуверенность ~15°)
- R[6,6] = 3.05e-4 (angle.dat точен до ~1°)
- K[6,6] ≈ 0.0685 / (0.0685 + 3.05e-4) ≈ 0.996

Фильтр доверяет angle.dat на 99.6%: x[6] += 0.996 · 0.1 ≈ 0.0996 рад.

### 13.3. Оценка смещения гироскопа (косвенная)

Смещение гиро bg оценивается через связь с ошибкой скорости:
- Инновация по скорости → K[9..11, 3..5] ≠ 0
- K[9..11, 3..5] обычно мала (косвенная связь)
- bg оценивается за десятки секунд

---

## 14. Руководство по настройке

### 14.1. Общий принцип

- **Q увеличивают**, когда модель неточная (шумные датчики, вибрации)
- **Q уменьшают**, когда модель точная (стабильная платформа, точные датчики)
- **R увеличивают**, когда измерения неточные (multipath, слабый сигнал)
- **R уменьшают**, когда измерения точные (открытое небо, RTK)

### 14.2. Начальная настройка

1. Определить точность датчиков (из datasheet)
2. Задать R = (точность измерения)²
3. Задать Q = (шум модели)² · T
4. Задать P₀ = (неопределённость начальных оценок)²
5. Проверить поведение на записанных данных
6. Подстроить σ_g, σ_a, σ_bg, σ_ba по характеру ошибок

### 14.3. Сводная таблица влияния параметров

| Параметр | ↑ Увеличить | ↓ Уменьшить |
|----------|-------------|-------------|
| σ_g (Q гиро) | Курс корректируется сильнее, но шумнее | Курс стабильнее, но коррекция слабее |
| σ_a (Q акселя) | Скорость корректируется сильнее, но шумнее | Скорость стабильнее, но «плывёт» |
| σ_bg (Q dr gyro) | bg оценивается быстрее, но шумнее | bg оценивается медленнее, но стабильнее |
| σ_ba (Q dr accel) | ba оценивается быстрее, но шумнее | ba оценивается медленнее, но стабильнее |
| σ_pos (R коорд) | Координаты «плывут» по БИНС | Координаты «прилипают» к GPS |
| σ_h (R высота) | Высота «плывёт» по БИНС | Высота «прилипает» к GPS |
| σ_v (R скорость) | Скорость «плывёт» по БИНС | Скорость «прилипает» к GPS |
| σ_ang (R углы) | Уги «плывут» по БИНС | Уги «прилипают» к angle.dat |
| P₀[6,6] (курс) | Быстрая коррекция начального курса | Медленная коррекция начального курса |
| P₀[3..5,3..5] (скорость) | Быстрая коррекция начальной скорости | Медленная коррекция начальной скорости |
| T (шаг) | Ошибки растут быстрее, Q больше | Ошибки растут медленнее, Q меньше |

### 14.4. Типичные ошибки настройки

- Слишком маленький Q → фильтр «не помнит» ошибки, коррекция слабая
- Слишком большой Q → фильтр «не помнит» ошибки, коррекция сильная, но шумная
- Слишком маленький R → результат «дёргается» за шумом измерений
- Слишком большой R → результат «плывёт» по модели, измерения игнорируются

### 14.5. Влиание каждого параметра

#### σ_g (Q[6..8, 6..8]) — шум гироскопа

| Изменение | Эффект |
|-----------|--------|
| Увеличить (2×) | P[6..8,6..8] растёт быстрее → K[6..8,*] увеличивается → коррекция по angle.dat сильнее. Но P растёт быстрее между коррекциями. |
| Уменьшить (2×) | P[6..8,6..8] растёт медленнее → K[6..8,*] уменьшается → коррекция слабее. Курс стабильнее между коррекциями. |

#### σ_a (Q[3..5, 3..5]) — шум акселерометра

| Изменение | Эффект |
|-----------|--------|
| Увеличить (2×) | P[3..5,3..5] растёт быстрее → K[3..5,*] увеличивается → коррекция по GPS скорости сильнее. Но скорость «шумная» между коррекциями. |
| Уменьшить (2×) | P[3..5,3..5] растёт медленнее → K[3..5,*] уменьшается → коррекция слабее. Скорость плавнее. |

#### σ_bg (Q[12..14, 12..14]) — дрейф смещения гироскопа

| Изменение | Эффект |
|-----------|--------|
| Увеличить (10×) | bg оценивается быстрее, но шумнее. |
| Уменьшить (10×) | bg оценивается медленнее, но стабильнее. |

#### σ_ba (Q[9..11, 9..11]) — дрейф смещения акселерометра

| Изменение | Эффект |
|-----------|--------|
| Увеличить (10×) | ba оценивается быстрее, но шумнее. |
| Уменьшить (10×) | ba оценивается медленнее, но стабильнее. |

#### σ_pos (R[0..1, 0..1]) — точность GPS по координатам

| Изменение | Эффект |
|-----------|--------|
| Увеличить (2×) | Координаты «плывут» по БИНС. K[0..1,*] уменьшаются. |
| Уменьшить (2×) | Координаты «прилипают» к GPS. K[0..1,*] увеличиваются. |

#### σ_v (R[3..5, 3..5]) — точность GPS по скорости

| Изменение | Эффект |
|-----------|--------|
| Увеличить (2×) | Скорость «плывёт» по БИНС. K[3..5,*] уменьшаются. |
| Уменьшить (2×) | Скорость «прилипает» к GPS. K[3..5,*] увеличиваются. |

#### σ_ang (R[6..8, 6..8]) — точность angle.dat

| Изменение | Эффект |
|-----------|--------|
| Увеличить (2×) | Уги «плывут» по БИНС. K[6..8,*] уменьшаются. |
| Уменьшить (2×) | Уги «прилипают» к angle.dat. K[6..8,*] увеличиваются. |

#### T — шаг дискретизации

| Изменение | Эффект |
|-----------|--------|
| Увеличить (2×) | F и Q растут → ошибки распространяются сильнее за шаг. |
| Уменьшить (2×) | F и Q уменьшаются → ошибки распространяются медленнее. Точнее интегрирование. |

---

## Приложение A: Источники кода

| Матрица/Функция | Файл |
|-----------------|------|
| F-матрица (Fj_matrix) | src/ins/ins_filter.h:91-147 |
| Q-матрица (Qj_matrix) | src/ins/ins_filter.h:35-59 |
| R-матрица (Rj_matrix) | src/ins/ins_filter.h:63-78 |
| H-матрица (H_matrix) | src/math_lib/matrix_ops.h:220-228 |
| Предсказание (predict) | src/ins/ins_filter.h:151-161 |
| Коррекция (correct) | src/ins/ins_filter.h:168-200 |
| Начальная P₀ (initialCovariance) | src/navigation/aligner.h:29-50 |
| Шаг счисления (step) | src/navigation/trajectory.h:96-188 |
| Интегрирование скоростей | src/navigation/position_calc.h:70-74 |
| Интегрирование координат | src/navigation/position_calc.h:78-93 |
| Интегрирование углов | src/ins/attitude_calc.h:63-71 |
| bodyToNavMatrix | src/math_lib/transformations.h:44-61 |
