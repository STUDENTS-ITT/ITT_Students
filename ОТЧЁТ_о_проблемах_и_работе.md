# Отчёт о проблемах и проделанной работе

Кодобаза: `C:\ITT_Students-add-kalman-report`
Дата: 2026-09-04

Данный отчёт описывает:

1. Глобальный анализ корректности формул фильтра Калмана (этапы предсказания и корректировки).
2. Проверку знаковых конвенций относительно базовой ветки `C:\ITT_Students`.
3. Внесённые правки и их обоснование.
4. Известные ограничения, не требующие правки.

---

## 1. Ключевой вывод: знаковые «ошибки» — НЕ ошибки

Ранее, при сравнении с базовой веткой `C:\ITT_Students`, были зафиксированы 3 знаковых расхождения
в F-матрице и в `eulerRates()`:

| Место | `add-kalman-report` | `ITT_Students` |
|-------|---------------------|----------------|
| `n_psi` (ось поворота курса) | `{0,-1,0}` (вниз) | `{0,1,0}` (вверх) |
| `n_theta` восточная компонента | `−sin(ψ)` | `+sin(ψ)` |
| `e_rate[0][1]`, `e_rate[0][2]` | `−cg/cth`, `+sg/cth` | `+cg/cth`, `−sg/cth` |
| `eulerRates().heading_dot` | минус | без минуса |

**Вывод после глубокой проверки:** эти расхождения — **не ошибки**, а разная конвенция знака курса.

- `add-kalman-report` использует **навигационную конвенцию**: курс растёт по часовой стрелке
  (0 = Север, 90° = Восток). Это подтверждают:
  - комментарий в `eulerRates()` (attitude_calc.h);
  - C-матрица вращения: при ψ=90° нос смотрит на Восток;
  - физика: положительный `wy` (правой рукой) — поворот против часовой → курс **уменьшается** → нужен минус.
- `ITT_Students` реализует курс против часовой стрелки, что противоречит собственным комментариям
  (заявлено «по часовой»), а его алгоритм выставки по угловой скорости Земли **сломан** — выдаёт
  начальный курс, зависящий от широты (на широте 55° вместо 0° даёт −35°).

**Итог:** знаковые члены в `add-kalman-report` корректны и самосогласованы, **правка не требуется**.

---

## 2. Глобальный анализ формул фильтра Калмана

### 2.1. Этап предсказания (time update)

```cpp
x = F · x
P = F · P · Fᵀ + Q
```

- `F = I + A·T` — дискретизация первого порядка переходной матрицы. Стандартно и корректно.
- `x = F·x` — распространение вектора ошибок. Корректно.
- `Q = σ²·T` — модель случайного блуждания для шумов процесса. Корректная дискретизация.
- Размерности матриц (15×15) согласованы во всех умножениях.

**Выявленный пробел:** модель ошибок в F была неполной относительно заявленной блочной схемы
(комментарий `δVė = 2ω_Zem·sin(φ)·δVn`). Члены Кориолиса `2·Ω×δV` отсутствовали.
**Исправлено** (см. п. 4.1).

### 2.2. Этап корректировки (measurement update)

```cpp
z = БИНС − СНС                 (инновация)
K = P·Hᵀ·(H·P·Hᵀ + R)⁻¹
x = x + K·(z − H·x)
P = (I − K·H)·P
```

- Структура `K = P·Hᵀ·S⁻¹`, `S = H·P·Hᵀ + R` — стандартная. Корректно.
- `H` — 9×15, измеряются первые 9 компонент состояния. Соответствует вектору измерений. Корректно.
- Угловые инновации нормализуются в диапазон [−π, π] — предусмотрено.
- Обновление ковариации в простой форме `(I − K·H)·P` (не Joseph). Математически корректно,
  но при плохой обусловленности может терять симметрию — приемлемо, не требует правки.

**Замечание по надёжности:** обращение матрицы `return_matrix()` при сингулярной `S` возвращает
единичную матрицу → коэффициент усиления становится `K = P·Hᵀ` (скачок). Это не ошибка формулы,
но потенциально опасное поведение. Оставлено как **известное ограничение** (для штатных данных не
возникает).

### 2.3. Известные ограничения по состоянию

- В F отсутствуют транспортно-частотные (нелинейные) связи скоростей `ω̂×δV` и позиционные члены
  `2Ω·cosφ·V·δφ`. Для низкоскоростного аппарата с частым обновлением СНС (10 Гц) их вклад
  пренебрежимо мал. Добавлены только Кориолисовы линейные члены (п. 4.1).

---

## 3. Проблемы с данными (angle.dat)

Документация проекта (`analysis_full_program_report.md`, `analysis_angle_dat.md`) однозначно
указывает: **углы angle.dat задаются в градусах** (`roll`, `pitch`, `yaw`; формат
`time_s | timestamp_ns | roll | pitch | yaw`).

В `add-kalman-report` углы читались «как есть» (без пересчёта `DEG_TO_RAD`), т.е. трактовались как
радианы. При реальных градусных значениях (например, 120.5°) это давало абсурдные эталонные углы
(120.5 рад вместо 2.1 рад), что портило эталон `reference.txt` и коррекцию курса.

Это — **реальный баг**. Исправлено (п. 4.2).

---

## 4. Внесённые правки

### 4.1. F-матрица: добавлены Кориолисовы связи скоростей

Файл: `src/ins/ins_filter.h`, функция `Fj_matrix()`.

Добавлены линейные члены Кориолиса из уравнения `V̇ = f_н − a_вред`, где `a_вред` содержит `2·Ω×V`:

```
F[3,5] (δV̇n ← δVe) = −2Ω·sin(φ)·T
F[4,5] (δV̇h ← δVe) = +2Ω·cos(φ)·T
F[5,3] (δV̇e ← δVn) = +2Ω·sin(φ)·T
```

Обоснование знаков:
- `a_вред_E = −2Ω·sinφ·Vn + …` → `V̇e = … + 2Ω·sinφ·Vn` → `δV̇e/δVn = +2Ω·sinφ` ✓ (совпадает с комментарием).
- `a_вред_N = +2Ω·sinφ·Ve + …` → `V̇n = … − 2Ω·sinφ·Ve` → `δV̇n/δVe = −2Ω·sinφ`.
- `a_вред_H = −2Ω·cosφ·Ve + …` → `V̇h = … + 2Ω·cosφ·Ve` → `δV̇h/δVe = +2Ω·cosφ`.

Члены малы для низкоскоростного аппарата, но делают модель ошибок полной и согласованной
с заявленной блочной схемой.

### 4.2. angle.dat: перевод углов из градусов в радианы

Файл: `src/data_io/data_reader.cpp`, функция `SnsReader::readNextAngle()`.

```cpp
ang_roll_curr_  = std::stod(ang[ANG_COL_ROLL])  * DEG_TO_RAD;
ang_pitch_curr_ = std::stod(ang[ANG_COL_PITCH]) * DEG_TO_RAD;
ang_yaw_curr_   = std::stod(ang[ANG_COL_YAW])   * DEG_TO_RAD;
```

Обновлены комментарии в `data_reader.cpp` и `data_reader.h`.

### 4.3. Коррекция курса при отсутствии angle.dat

Файлы: `src/navigation/trajectory.h`, `src/main.cpp`.

Проблема: при отсутствии `angle.dat` поле `ref.heading` равно 0 (заполняется в `SnsReader`).
Инновация курса `z = БИНС − 0` вынуждала фильтр **тянуть истинный курс к нулю**, ломая счисление.

Решение: в вектор измерений в `trajectory.h` курс подставляется только при наличии эталона курса:

```cpp
const Vector sns = {..., has_heading_ref ? ref.heading : st.att.heading, ...};
```

- `has_heading_ref == true` → курс корректируется по `angle.dat`;
- `false` → замер курса считается отсутствующим (инновация = 0), курс не корректируется.

Флаг передаётся из `main.cpp` (`has_angle`) в новый параметр `step(..., bool has_heading_ref)`.

---

## 5. Проверка синтаксиса

Сборка через Ninja/MinGW в данной рабочей среде сломана («unknown error»), поэтому корректность
проверена компиляцией в режиме проверки синтаксиса:

```
g++ -std=c++17 -fsyntax-only -Isrc src/main.cpp
```

Результат: **успешно (exit 0)** — все изменённые файлы (`ins_filter.h`, `data_reader.cpp`,
`data_reader.h`, `trajectory.h`, `main.cpp`) синтаксически корректны.

---

## 6. Итоги

- Знаковые расхождения относительно `C:\ITT_Students` признаны корректными (конвенция курса),
  изменений не требуется.
- Исправлены 3 реальные глобальные проблемы:
  1. Неполная F-модель ошибок (добавлены Кориолисовы связи скоростей).
  2. Некорректные единицы angle.dat (градусы трактовались как радианы).
  3. Неверная коррекция курса при отсутствии angle.dat (тянула курс к нулю).
- Написана полная документация фильтра: `analysis_kalman_matrices.md` (матрицы Q, R, F, H,
  этапы predict/correct, чувствительности).

## 7. Известные ограничения (не правятся)

1. `return_matrix()` возвращает единичную матрицу при сингулярной S (см. п. 2.2).
2. Обновление P не в Joseph-форме (см. п. 2.2).
3. В F отсутствуют нелинейные транспортно-частотные связи `ω̂×δV` (пренебрежимо малы).
4. Правки проверены синтаксически; полный прогон на данных требует исправной сборки.
