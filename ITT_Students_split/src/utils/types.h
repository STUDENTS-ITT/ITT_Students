#pragma once

// Базовые типы линейной алгебры.
// Матрица хранится построчно в одномерном массиве, число столбцов
// передаётся в функции отдельным аргументом.

#include <vector>

using Matrix = std::vector<double>;
using Vector = std::vector<double>;
