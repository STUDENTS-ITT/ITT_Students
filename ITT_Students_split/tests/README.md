# Тесты

Каталог для тестов модулей: `math_lib`, `ins`, `navigation`, `data_io`.

Проверка после рефакторинга: собрать `imitator`, запустить его в каталоге
с `imu.dat`, `gps.dat`, `angle.dat` и сравнить `kalman15_line2.txt` и `d_1.txt`
с результатами исходного `main.cpp` — файлы должны совпадать побайтово.
