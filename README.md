# Документ: Методы сглаживания временных рядов
## Теоретическое обоснование, сравнительный анализ и связь с фильтром Калмана

**Назначение:** Описание семейства линейных фильтров для сглаживания временных рядов в задачах обработки сигналов, статистического анализа и мониторинга.

---

## 1. Введение

Сглаживание временных рядов является фундаментальной операцией при обработке зашумленных данных. Цель — восстановить полезный сигнал, минимизируя влияние случайных флуктуаций. В данном документе рассматриваются четыре метода, различающиеся по принципу усреднения, статистическим свойствам и областям применения:

1. **Boxcar** — скользящее среднее с прямоугольным окном.  
2. **Progressive** — кумулятивное (расширяющееся) среднее.  
3. **Hybrid** — комбинированный метод с зональным переключением.  
4. **Gaussian** — взвешенное среднее с гауссовским ядром.

Для каждого метода приведены:
- математическая модель,
- статистические характеристики (несмещённость, дисперсия, состоятельность),
- преимущества и недостатки,
- связь с фильтром Калмана.

---

## 2. Обозначения и сокращения

В настоящем документе используются следующие математические обозначения и термины:

### 2.1. Основные символы

| Символ | Описание |
|--------|----------|
| \( x_i \) | Наблюдаемое значение временного ряда в момент \( i \) |
| \( s_i \) | Истинное (полезное) значение сигнала в момент \( i \) |
| \( \hat{s}_i \) | Оценка сигнала (сглаженное значение) |
| \( \varepsilon_i \) | Случайная ошибка (шум) в момент \( i \) |
| \( i \) | Индекс временного ряда (\( i = 0, 1, \dots, n-1 \)) |
| \( n \) | Общее число наблюдений |
| \( W \) | Размер окна (ширина фильтра) |
| \( h \) | Радиус окна: \( h = \lfloor W/2 \rfloor \) |
| \( \sigma^2 \) | Дисперсия шума измерений |
| \( \mu \) | Истинное математическое ожидание (для стационарного сигнала) |

### 2.2. Статистические операторы

| Символ | Описание |
|--------|----------|
| \( E[\cdot] \) | Оператор математического ожидания |
| \( D[\cdot] \) | Оператор дисперсии |
| \( \text{Cov}[\cdot, \cdot] \) | Ковариация случайных величин |
| \( \text{MSE} \) | Среднеквадратическая ошибка (Mean Squared Error) |
| \( \xrightarrow{p} \) | Сходимость по вероятности |
| \( \xrightarrow{d} \) | Сходимость по распределению |
| \( \mathcal{N}(\mu, \sigma^2) \) | Нормальное распределение со средним \( \mu \) и дисперсией \( \sigma^2 \) |

### 2.3. Специфические обозначения методов

| Символ | Описание |
|--------|----------|
| \( b_i \) | Оценка сигнала методом Boxcar в точке \( i \) |
| \( p_i \) | Оценка сигнала методом Progressive в точке \( i \) |
| \( \alpha_i \) | Вес линейной интерполяции в гибридном методе |
| \( k_j \) | Весовой коэффициент гауссовского ядра |
| \( Z \) | Нормировочная константа для гауссовского ядра |
| \( \sigma_G \) | Параметр ширины гауссовского ядра (в методах) |

### 2.4. Обозначения фильтра Калмана

| Символ | Описание |
|--------|----------|
| \( F_k \) | Матрица (или коэффициент) перехода состояния |
| \( H_k \) | Матрица (или коэффициент) наблюдения |
| \( Q_k \) | Дисперсия шума процесса |
| \( R_k \) | Дисперсия шума измерений |
| \( P_k \) | Ковариационная матрица ошибки оценивания |
| \( K_k \) | Коэффициент усиления Калмана (оптимальный вес) |

### 2.5. Сокращения

| Сокращение | Расшифровка |
|------------|-------------|
| ЗБЧ | Закон больших чисел |
| ЦПТ | Центральная предельная теорема |
| ЛФ | Линейный фильтр |
| МСК | Среднеквадратический критерий |

---

## 3. Математическая постановка задачи

Пусть задан временной ряд:

\[
\{x_i\}_{i=0}^{n-1}, \quad x_i = s_i + \varepsilon_i,
\]

где:
- \( s_i \) — детерминированный полезный сигнал,
- \( \varepsilon_i \) — случайная составляющая (шум),
- \( E[\varepsilon_i] = 0 \),
- \( D[\varepsilon_i] = \sigma^2 \) (дисперсия шума),
- шум предполагается некоррелированным: \( \text{Cov}[\varepsilon_i, \varepsilon_j] = \sigma^2 \delta_{ij} \).

Задача — построить оценку \( \hat{s}_i \) сигнала \( s_i \), обладающую минимальной среднеквадратической ошибкой (MSE) при заданных ограничениях на вычислительную сложность и адаптивность.

---

## 4. Метод 1: Boxcar (прямоугольное скользящее окно)

### 4.1. Определение

Оценка сигнала в точке \( i \) вычисляется как среднее арифметическое значений в фиксированном окне шириной \( W \):

\[
\hat{s}_i = \frac{1}{m_i} \sum_{j = i - h}^{i + h} x_j, \quad h = \left\lfloor \frac{W}{2} \right\rfloor,
\]

где \( m_i \) — число наблюдений, попавших в окно (на краях ряда \( m_i < W \)).

В центральной области:

\[
\hat{s}_i = \frac{1}{W} \sum_{j = -h}^{h} x_{i+j}.
\]

### 4.2. Статистические свойства

- **Несмещённость**: для линейного сигнала \( s_i = a i + b \) оценка несмещена:
  \[
  E[\hat{s}_i] = s_i.
  \]
  Для нелинейного сигнала возникает смещение, пропорциональное второй производной:
  \[
  \text{Bias} \approx \frac{h^2}{3} s''_i.
  \]

- **Дисперсия**:
  \[
  D[\hat{s}_i] = \frac{\sigma^2}{W}.
  \]

- **Среднеквадратическая ошибка**:
  \[
  \text{MSE} = \frac{\sigma^2}{W} + \left( \frac{h^2}{3} s''_i \right)^2.
  \]

### 4.3. Преимущества и недостатки

| Преимущества | Недостатки |
|--------------|------------|
| Простота реализации | Смещение на нелинейных участках |
| Низкая дисперсия при большом окне | Краевые эффекты (усечение окна) |
| Легко управлять полосой пропускания | Неэффективен при цветном шуме |
| Не требует априорной модели | Фиксированная память (не учитывает долгосрочный тренд) |

### 4.4. Связь с фильтром Калмана

Boxcar является частным случаем фильтра Калмана при:
- модели постоянного сигнала (\( F = 1, H = 1 \)),
- бесконечной дисперсии шума процесса (\( Q \to \infty \)),
- дисперсии измерения \( R = \sigma^2 \).

В этом случае фильтр полностью доверяет измерениям и не использует прогнозную модель.

---

## 5. Метод 2: Progressive (кумулятивное среднее)

### 5.1. Определение

На каждом шаге \( i \) оценка вычисляется как среднее всех предыдущих наблюдений:

\[
\hat{s}_i = \frac{1}{i+1} \sum_{j=0}^{i} x_j.
\]

Рекурсивная форма:

\[
\hat{s}_i = \hat{s}_{i-1} + \frac{1}{i+1} (x_i - \hat{s}_{i-1}).
\]

### 5.2. Статистические свойства

- **Несмещённость** для стационарного сигнала (\( s_i = \mu \)):
  \[
  E[\hat{s}_i] = \mu.
  \]

- **Состоятельность**:
  \[
  \hat{s}_i \xrightarrow{p} \mu \quad \text{при } i \to \infty.
  \]

- **Дисперсия**:
  \[
  D[\hat{s}_i] = \frac{\sigma^2}{i+1} \to 0.
  \]

- **Асимптотическая нормальность**:
  \[
  \sqrt{i+1} (\hat{s}_i - \mu) \xrightarrow{d} \mathcal{N}(0, \sigma^2).
  \]

### 5.3. Преимущества и недостатки

| Преимущества | Недостатки |
|--------------|------------|
| Минимальная асимптотическая дисперсия | Крайне низкая скорость реакции на тренды |
| Отсутствие краевых эффектов | Смещение при нестационарности |
| Рекурсивное обновление \( O(1) \) | Чувствительность к выбросам (постоянный вес) |
| Состоятельность в стационарном случае | Неэффективен при изменении среднего |

### 5.4. Связь с фильтром Калмана

Progressive — это точное решение фильтра Калмана для модели:
- \( F = 1, H = 1 \),
- \( Q = 0 \) (полное доверие к модели постоянного сигнала),
- \( R = \sigma^2 \),
- начальная ковариация \( P_0 \to \infty \).

Коэффициент усиления Калмана принимает вид \( K_i = \frac{1}{i+1} \), что эквивалентно прогрессивному усреднению.

---

## 6. Метод 3: Hybrid (гибридный фильтр)

### 6.1. Определение

Метод объединяет Boxcar и Progressive с зональным переключением:

\[
\hat{s}_i =
\begin{cases}
b_i, & i < W,\\
(1 - \alpha_i) b_i + \alpha_i p_i, & W \le i < 2W,\\
p_i, & i \ge 2W,
\end{cases}
\]

где:
- \( b_i \) — оценка Boxcar,
- \( p_i \) — оценка Progressive,
- \( \alpha_i = \frac{i - W}{W} \in [0,1] \) — вес линейного перехода.

### 6.2. Статистические свойства

- В зоне перехода оценка является выпуклой комбинацией:
  \[
  D[\hat{s}_i] = (1-\alpha)^2 D[b_i] + \alpha^2 D[p_i] + 2\alpha(1-\alpha)\text{Cov}(b_i, p_i).
  \]

- Асимптотически (при \( i \ge 2W \)) совпадает с Progressive.

- MSE ограничена сверху максимумом из MSE обоих методов, что гарантирует устойчивость.

### 6.3. Преимущества и недостатки

| Преимущества | Недостатки |
|--------------|------------|
| Устойчивый старт (Boxcar) | Разрыв производной на стыках зон |
| Плавный переход без скачков | Зависимость от выбора \( W \) |
| Асимптотическая состоятельность | Требует предварительного расчёта Boxcar |
| Уменьшение дисперсии с ростом \( i \) | Не является оптимальным в статистическом смысле |

### 6.4. Связь с фильтром Калмана

Hybrid можно интерпретировать как **фильтр Калмана с переменной дисперсией шума процесса** \( Q_k \):
- при \( i < W \): \( Q \to \infty \) (доверие к измерениям),
- при \( i \ge 2W \): \( Q = 0 \) (полное доверие к постоянной модели).

Однако в отличие от фильтра Калмана, переключение происходит не адаптивно, а по заранее заданной временной схеме, что снижает оптимальность.

---

## 7. Метод 4: Gaussian (гауссовское сглаживание)

### 7.1. Определение

Используется ядро Гаусса:

\[
k_j = \frac{1}{Z} \exp\left( -\frac{(j - h)^2}{2\sigma_G^2} \right), \quad j = 0, \dots, W-1,
\]

где:
- \( h = \lfloor W/2 \rfloor \),
- \( \sigma_G = W / 6 \),
- \( Z = \sum_{j=0}^{W-1} \exp\left( -\frac{(j - h)^2}{2\sigma_G^2} \right) \) — нормировочная константа.

Оценка:

\[
\hat{s}_i = \sum_{j=0}^{W-1} k_j \cdot \tilde{x}_{i - h + j},
\]

с предварительным зеркальным отражением краёв.

### 7.2. Статистические свойства

- **Несмещённость**: для сигналов, разлагаемых в ряд Тейлора до 3-го порядка:
  \[
  \text{Bias} = O(\sigma_G^4 s''''_i).
  \]

- **Дисперсия**:
  \[
  D[\hat{s}_i] = \sigma^2 \sum_{j=0}^{W-1} k_j^2 \approx \frac{\sigma^2}{2\sigma_G\sqrt{\pi}}.
  \]

- **Частотная характеристика**:
  \[
  H(\omega) = \exp\left( -\frac{\sigma_G^2 \omega^2}{2} \right),
  \]
  что обеспечивает монотонное подавление высоких частот без боковых лепестков.

### 7.3. Преимущества и недостатки

| Преимущества | Недостатки |
|--------------|------------|
| Минимальное смещение для гладких сигналов | Размытие острых пиков |
| Отсутствие фазового сдвига | Высокая вычислительная сложность |
| Частотная характеристика без осцилляций | Чувствительность к выбору \( \sigma_G \) |
| Бесконечная дифференцируемость | Игнорирование долгосрочных трендов |

### 7.4. Связь с фильтром Калмана

Gaussian является **стационарным фильтром Винера** для процессов с гауссовским спектром. Для стационарных сигналов он даёт результат, эквивалентный установившемуся фильтру Калмана с постоянными коэффициентами. Однако в отличие от фильтра Калмана, он не адаптируется к изменению статистик сигнала во времени.

---

## 8. Сравнительная таблица

| Критерий | Boxcar | Progressive | Hybrid | Gaussian |
|----------|--------|-------------|--------|----------|
| **Несмещённость** (стац.) | Да (в центре) | Да | В пределе | Да (с отражением) |
| **Состоятельность** | Нет | Да | Да | Нет |
| **Дисперсия оценки** | \( \sigma^2/W \) | \( \sigma^2/(i+1) \) | Убывает | \( \sigma^2 \sum k_j^2 \) |
| **Асимптотическая эффективность** | Нет | Да | Частично | Нет |
| **Чувствительность к тренду** | Низкая | Высокая | Средняя | Низкая |
| **Краевые эффекты** | Сильные | Отсутствуют | Умеренные | Слабые |
| **Сложность** | \( O(nW) \) | \( O(n) \) | \( O(nW) \) | \( O(nW) \) |
| **Связь с фильтром Калмана** | \( Q \to \infty \) | \( Q = 0 \) | Переменный \( Q \) | Стационарный фильтр Винера |

---

## 9. Рекомендации по выбору метода

| Условия применения | Рекомендуемый метод |
|---------------------|----------------------|
| Стационарный сигнал, длинная выборка | Progressive |
| Быстрая реакция на изменения, локальное сглаживание | Boxcar (малое окно) |
| Требуется гладкий старт и состоятельность в пределе | Hybrid |
| Важна гладкость и отсутствие фазовых искажений | Gaussian |
| Доступна модель процесса и статистики шумов | Фильтр Калмана (предпочтительно) |

---

## 10. Заключение

Все рассмотренные методы являются линейными фильтрами и могут быть интерпретированы как упрощённые или частные случаи фильтра Калмана. Выбор конкретного метода определяется:
- природой сигнала (стационарность, гладкость, наличие тренда),
- требованиями к скорости реакции и точности,
- вычислительными ограничениями.

Фильтр Калмана остаётся универсальным эталоном для задач оптимальной фильтрации, однако в условиях неопределённости и ограниченных ресурсов методы Boxcar, Progressive, Hybrid и Gaussian предоставляют эффективные практические альтернативы.
