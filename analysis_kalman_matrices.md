# Матрицы фильтра Калмана — полный разбор

Фильтр Калмана 15-го порядка (угловатый, error-state). Каждая матрица разобрана поэлементно с физической интерпретацией.

---

## 1. Вектор состояний x (15×1)

Это **вектор ошибок** (не само состояние навигации). Каждый элемент — разность между истинным и оценённым значением:

| Индекс | Обозначение | Физический смысл | Единицы |
|--------|-------------|-------------------|---------|
| x[0] | δφ | Ошибка широты | рад |
| x[1] | δλ | Ошибка долготы | рад |
| x[2] | δh | Ошибка высоты | м |
| x[3] | δVn | Ошибка скорости на север | м/с |
| x[4] | δVh | Ошибка вертикальной скорости | м/с |
| x[5] | δVe | Ошибка скорости на восток | м/с |
| x[6] | δψ | Ошибка курса | рад |
| x[7] | δθ | Ошибка тангажа | рад |
| x[8] | δφ | Ошибка крена | рад |
| x[9..11] | δba | Ошибка смещения акселерометра (X, Y, Z) | м/с² |
| x[12..14] | δbg | Ошибка смещения гироскопа (X, Y, Z) | рад/с |

После коррекции x обнуляется — все ошибки «применены» к состоянию.

Формула коррекции состояния:
```
старое_значение -= x[соответствующий_индекс]    (координаты, скорости, углы)
старое_значение += x[соответствующий_индекс]    (смещения датчиков)
```

---

## 2. Начальная ковариация P₀ (15×15, диагональная)

Каждый диагональный элемент — **квадрат неопределённости** начальной оценки. Физический смысл: «насколько мы не уверены» в каждой компоненте.

| Элемент | Значение | Формула | Физический смысл |
|---------|----------|---------|-------------------|
| P[0,0] | 6.14e-9 | (50м / R_earth)² | Широта: неопределённость ~50 м |
| P[1,1] | 6.14e-9 | (50м / R_earth)² | Долгота: неопределённость ~50 м |
| P[2,2] | 100 | (10м)² | Высота: неопределённость ~10 м |
| P[3,3] | 6.25 | (2.5 м/с)² | Скорость N: неопределённость ~2.5 м/с |
| P[4,4] | 6.25 | (2.5 м/с)² | Скорость H: неопределённость ~2.5 м/с |
| P[5,5] | 6.25 | (2.5 м/с)² | Скорость E: неопределённость ~2.5 м/с |
| P[6,6] | 6.85e-2 | (15° × π/180)² | Курс: неопределённость ~15° |
| P[7,7] | 3.05e-6 | (0.1° × π/180)² | Тангаж: неопределённость ~0.1° |
| P[8,8] | 3.05e-6 | (0.1° × π/180)² | Крен: неопределённость ~0.1° |
| P[9,9] | 9e-6 | (0.003 м/с²)² | Смещение акселя X: ~0.003 м/с² |
| P[10,10] | 9e-6 | | Смещение акселя Y |
| P[11,11] | 9e-6 | | Смещение акселя Z |
| P[12,12] | 1e-8 | (1e-4 рад/с)² | Смещение гиро X: ~0.006°/с |
| P[13,13] | 1e-8 | | Смещение гиро Y |
| P[14,14] | 1e-8 | | Смещение гиро Z |

Все недиагональные элементы = 0 (нет начальной корреляции между ошибками).

Чем больше диагональный элемент → тем **менее уверены** в оценке → фильтр будет сильнее корректировать по измерениям.

---

## 3. Матрица перехода F (15×15)

Описывает, как ошибки **эволюционируют** за один шаг dt. Структура: `F = I + F_c · T`, где F_c — непрерывная модель ошибок, T — шаг дискретизации.

Диагональ = 1.0 (единичная матрица): на каждом шаге ошибки «остаются» + прибавляется дрейф через недиагональные элементы.

### Блок 1: Координаты ← Скорости (строки 0-2, столбцы 3-5)

| Элемент | Значение | Физический смысл |
|---------|----------|-------------------|
| F[0,3] | T / R | Широта растёт пропорционально скорости на север: δφ̇ = δVn / R |
| F[1,5] | T / (R·cosφ) | Долгота растёт пропорционально скорости на восток: δλ̇ = δVe / (R·cosφ) |
| F[2,4] | T | Высота растёт пропорционально вертикальной скорости: δḣ = δVh |

Физика: если скорость на север завышена на 1 м/с, то за T секунд координата ошибается на T/R радиан (~8e-8 рад при T=0.005 с). За 1 секунду (200 шагов) — ~1.6e-5 рад ≈ 1 метр.

Защита от сингулярности на полюсе: `cos(lat)` заменяется на `fmax(fabs(cos(lat)), 1e-10)`.

### Блок 2: Скорости ← Координаты (строка 4, столбец 2)

| Элемент | Значение | Физический смысл |
|---------|----------|-------------------|
| F[4,2] | T · 2g/R | Ошибка высоты → ошибка вертикальной скорости (гравитационный градиент) |

Физика: на высоте h ускорение свободного падения уменьшается приблизительно на 2g·h/R. Если высота ошиблась на δh, расчётное ускорение неверно на 2g·δh/R → ошибка вертикальной скорости растёт.

При h = 1000 м: δg/g ≈ 2·1000/6371000 ≈ 3e-4 (0.03%). Малая, но накапливается.

### Блок 3: Скорости ← Ориентация (строки 3-5, столбцы 6-8)

| Элемент | Значение | Физический смысл |
|---------|----------|-------------------|
| F[3+r, 6] | T · (n_psi × f_nav)[r] | Ошибка курса → ошибка скорости |
| F[3+r, 7] | T · (n_theta × f_nav)[r] | Ошибка тангажа → ошибка скорости |
| F[3+r, 8] | T · (n_gamma × f_nav)[r] | Ошибка крена → ошибка скорости |

Где:
- `n_psi = {0, 1, 0}` — ось вращения курса (вертикаль в навигационной СК)
- `n_theta = {sin(ψ), 0, cos(ψ)}` — ось вращения тангажа (горизонтальная, перпендикулярная курсу)
- `n_gamma = C[:,0]` — ось вращения крена (продольная ось тела в навигационной СК)

Физика: акселерометр измеряет перегрузку вдоль осей тела. Матрица C переводит это в навигационную СК. Если ориентация ошибочна на δψ, то акселерометр «видит» перегрузку в неверном направлении:

```
δV̇_n = -(V_e · cos(ψ) + V_n · sin(ψ)) · δψ  ≈  -f_e · δψ
δV̇_e =  (V_n · cos(ψ) - V_e · sin(ψ)) · δψ  ≈   f_n · δψ
```

Величина зависит от текущей перегрузки `f_nav`. При полёте с перегрузкой 1g (9.81 м/с²) ошибка курса 1° = 0.0175 рад даёт ошибку ускорения ~0.17 м/с² → за 1 секунду ошибка скорости ~0.17 м/с.

### Блок 4: Скорости ← Смещения акселерометра (строки 3-5, столбцы 9-11)

| Элемент | Значение | Физический смысл |
|---------|----------|-------------------|
| F[3, 9+j] | T · C[0,j] | Смещение акселя по оси j → ошибка скорости на север |
| F[4, 9+j] | T · C[1,j] | Смещение акселя по оси j → ошибка вертикальной скорости |
| F[5, 9+j] | T · C[2,j] | Смещение акселя по оси j → ошибка скорости на восток |

Физика: если акселерометр по оси X (продольная) завышает показания на ba_x, это проецируется в навигационную СК через матрицу C:

```
δV̇_n += C[0,0] · ba_x + C[0,1] · ba_y + C[0,2] · ba_z
```

При ba_x = 0.01 м/с² и C[0,0] = 1 (горизонтальный полёт на север): ошибка скорости на север растёт на 0.01 м/с².

### Блок 5: Ориентация ← Смещения гироскопа (строки 6-8, столбцы 12-14)

| Элемент | Значение | Физический смысл |
|---------|----------|-------------------|
| F[6, 12..14] | T · e_rate[0][0..2] | Смещение гиро → ошибка курса |
| F[7, 12..14] | T · e_rate[1][0..2] | Смещение гиро → ошибка тангажа |
| F[8, 12..14] | T · e_rate[2][0..2] | Смещение гиро → ошибка крена |

Где `e_rate` — связывающая матрица Эйлера (преобразование угловых скоростей тела в производные углов Эйлера):

```
e_rate = {{0,       cos(φ)/cos(θ),  -sin(φ)/cos(θ)},
          {0,       sin(φ),          cos(φ)},
          {1,       -tan(θ)cos(φ),   tan(θ)sin(φ)}}
```

Физика:
- Смещение гиро по оси Z (вертикаль тела) → вклад в курс через e_rate[0,2] = -sin(φ)/cos(θ)
- Смещение гиро по оси Y (поперечная) → вклад в тангаж через e_rate[1,2] = cos(φ)
- Смещение гиро по оси X (продольная) → вклад в крен через e_rate[2,0] = 1

При bg_x = 0.001 рад/с (≈0.06°/с): за 1 секунду крен ошибётся на 0.001 рад ≈ 0.06°. За 10 секунд — 0.6°.

Сингулярность при θ = ±90° (тангаж = ±90°): `cos(θ)` в знаменателе → защита через `fmax(fabs(cos(θ)), 1e-10)`.

---

## 4. Матрица шума процесса Q (15×15, диагональная)

Описывает **неопределённость модели** за один шаг T. Формула: `Q = σ² · T` для каждого компонента.

| Блок | σ | Значение Q | Физический смысл |
|------|---|------------|-------------------|
| Q[0..2, 0..2] | 0 | 0 | Координаты — детерминированы (шум = 0) |
| Q[3..5, 3..5] | σ_a = 3.05e-3 м/с²/√с | 4.65e-8 | Шум акселерометра → неопределённость скорости |
| Q[6..8, 6..8] | σ_g = 3.39e-4 рад/√с | 5.75e-10 | Шум гироскопа → неопределённость углов |
| Q[9..11, 9..11] | σ_ba = 2e-5 м/с²/√с | 2e-12 | Дрейф акселерометра |
| Q[12..14, 12..14] | σ_bg = 1.16e-5 рад/с/√с | 6.73e-13 | Дрейф гироскопа |

Физика: за каждый шаг T=0.005 с, неопределённость скорости вырастает на 4.65e-8 (м/с)² из-за шума акселерометра. За 200 шагов (1 сек) — на 9.3e-6 (м/с)², т.е. неопределённость скорости вырастает на ~0.003 м/с.

Накопление за 10 секунд (2000 шагов): 9.3e-5 (м/с)² → σ_V ≈ 0.01 м/с. Это согласуется с типичной точностью GNSS/INS.

---

## 5. Матрица наблюдения H (9×15)

Определяет, **какие компоненты состояния** мы измеряем напрямую.

```
H = [ I₉  |  0₉×6 ]
```

| Строка | H[j,j] | Что измеряем | Связь с x |
|--------|--------|--------------|-----------|
| 0 | 1 | Широта БИНС = широта СНС | x[0] — ошибка широты |
| 1 | 1 | Долгота БИНС = долгота СНС | x[1] — ошибка долготы |
| 2 | 1 | Высота БИНС = высота СНС | x[2] — ошибка высоты |
| 3 | 1 | Скорость N БИНС = скорость N СНС | x[3] — ошибка скорости N |
| 4 | 1 | Скорость H БИНС = скорость H СНС | x[4] — ошибка скорости H |
| 5 | 1 | Скорость E БИНС = скорость E СНС | x[5] — ошибка скорости E |
| 6 | 1 | Курс БИНС = курс СНС | x[6] — ошибка курса |
| 7 | 1 | Тангаж БИНС = тангаж СНС | x[7] — ошибка тангажа |
| 8 | 1 | Крен БИНС = крен СНС | x[8] — ошибка крена |

Столбцы 9-14 = 0: смещения акселя/гиро **ненаблюдаемы** напрямую. Они оцениваются косвенно (через F-матрицу и инновацию по скорости/углам).

Текущая реализация: строки 7,8 (тангаж/крен) фактически обнулены (инновация = 0), т.к. `sns[7] = st.att.pitch`, `sns[8] = st.att.roll`.

---

## 6. Матрица шума измерений R (9×9, диагональная)

Описывает **точность GPS и angle.dat**. Чем больше R → тем меньше доверия к измерению → тем слабее коррекция.

| Элемент | Значение | Формула | Физический смысл |
|---------|----------|---------|-------------------|
| R[0,0] | 6.14e-9 | (5м / R)² | Широта: точность GPS ~5 м (переведено в рад) |
| R[1,1] | 6.14e-9 | (5м / R)² | Долгота: точность GPS ~5 м |
| R[2,2] | 25 | (5м)² | Высота: точность GPS ~5 м |
| R[3,3] | 0.01 | (0.1 м/с)² | Скорость N: точность GPS ~0.1 м/с |
| R[4,4] | 0.01 | (0.1 м/с)² | Скорость H: точность GPS ~0.1 м/с |
| R[5,5] | 0.01 | (0.1 м/с)² | Скорость E: точность GPS ~0.1 м/с |
| R[6,6] | 3.05e-4 | (1° × π/180)² | Курс: точность angle.dat ~1° |
| R[7,7] | 3.05e-4 | (1° × π/180)² | Тангаж: точность angle.dat ~1° |
| R[8,8] | 3.05e-4 | (1° × π/180)² | Крен: точность angle.dat ~1° |

Физика R[0,0]: погрешность GPS 5 метров в радианах — это 5 / 6371000 ≈ 7.85e-7 рад. Дисперсия: (7.85e-7)² ≈ 6.14e-9 рад². Это огромное число в радианах, но разумное в метрах.

---

## 7. Коэффициент усиления Калмана K (15×9)

Вычисляется как: `K = P·H^T·(H·P·H^T + R)⁻¹`

Физический смысл каждого элемента K[i,j]: **насколько сильно инновация по j-й компоненте измерения влияет на i-ю компоненту состояния**.

| Блок K | Физический смысл |
|--------|-------------------|
| K[0..2, 0..2] | Инновация по координатам → коррекция координат |
| K[3..5, 3..5] | Инновация по скоростям → коррекция скоростей |
| K[6..8, 6..8] | Инновация по углам → коррекция углов |
| K[0..2, 3..5] | Инновация по скоростям → коррекция координат (кинематическая связь) |
| K[3..5, 0..2] | Инновация по координатам → коррекция скоростей (динамическая связь) |
| K[3..5, 6..8] | Инновация по углам → коррекция скоростей (через проекцию перегрузки) |
| K[6..8, 12..14] | Инновация по углам → оценка смещения гиро |
| K[9..14, *] | Любая инновация → оценка смещений акселя/гиро (косвенная) |

### Зависимость K от P и R

| Условие | K | Поведение |
|---------|---|-----------|
| P большая, R малая | K → H^T | Полная коррекция (не уверены в оценке, доверяем измерению) |
| P малая, R большая | K → 0 | Коррекция минимальна (уверены в оценке, не доверяем измерению) |
| P большая, R большая | K средний | Баланс между оценкой и измерением |

---

## 8. Ковариация P (15×15) — эволюция во времени

P обновляется на каждом шаге в два этапа:

### Предсказание: `P = F·P·F^T + Q`

- **F·P·F^T**: ошибки «распространяются» через модель динамики. Например:
  - Ошибка скорости δV → ошибка координат (через F[0,3], F[1,5])
  - Смещение гиро δbg → ошибка ориентации (через F[6..8, 12..14])
  - Ошибка ориентации → ошибка скорости (через F[3..5, 6..8])
- **Q**: добавляется неопределённость от шума датчиков. Без Q, P стремится к 0 (фильтр становится «слишком уверенным»).

### Коррекция: `P = (I - K·H)·P`

- **K·H**: «сжатие» неопределённости по измеренным компонентам
- Неизмеренные компоненты (ba, bg) уменьшаются **косвенно** (через недиагональные элементы P)

Физика P[6,6] (неопределённость курса):
- Между коррекциями: **растёт** (из-за шума гироскопа, Q[6,6])
- При коррекции по angle.dat: **уменьшается** (K[6,6] · P[6,6] · H[6,6])
- Без angle.dat: **только растёт** (нет прямого наблюдения курса)

---

## 9. Уравнения фильтра — итог

### Предсказание (на каждом такте ИМУ, 200 Гц):
```
x = F · x
P = F · P · F^T + Q
```

### Коррекция (при обновлении GPS, ~10 Гц):
```
z = БИНС − СНС                    (инновация)
S = H · P · H^T + R               (ковариация инновации)
K = P · H^T · S⁻¹                 (коэффициент усиления)
x = x + K · (z − H · x)           (коррекция состояния)
P = (I − K · H) · P               (коррекция ковариации)
```

### Применение коррекции к состоянию:
```
lat  -= x[0]    lon  -= x[1]    alt  -= x[2]
Vn   -= x[3]    Vh   -= x[4]    Ve   -= x[5]
ψ    -= x[6]    θ    -= x[7]    φ    -= x[8]
ba   += x[9..11]
bg   += x[12..14]
x = 0                                    (обнуление)
```

---

## 10. Числовые примеры

### Пример 1: Коррекция скорости

Допустим, инновация по скорости на север: δVn = 0.5 м/с (БИНС показывает 10.5 м/с, GPS — 10.0 м/с).

- P[3,3] = 6.25 (неуверенность в скорости ~2.5 м/с)
- R[3,3] = 0.01 (GPS точен до 0.1 м/с)
- K[3,3] ≈ P[3,3] / (P[3,3] + R[3,3]) ≈ 6.25 / 6.26 ≈ 0.998

Фильтр доверяет GPS на 99.8%: x[3] += 0.998 · 0.5 ≈ 0.499 м/с. Скорость корректируется почти полностью.

### Пример 2: Коррекция курса (с angle.dat)

Допустим, инновация по курсу: δψ = 0.1 рад (~5.7°).

- P[6,6] = 0.0685 (неуверенность в курсе ~15°)
- R[6,6] = 3.05e-4 (angle.dat точен до ~1°)
- K[6,6] ≈ 0.0685 / (0.0685 + 3.05e-4) ≈ 0.996

Фильтр доверяет angle.dat на 99.6%: x[6] += 0.996 · 0.1 ≈ 0.0996 рад.

### Пример 3: Оценка смещения гироскопа (косвенная)

Смещение гиро bg оценивается через связь с ошибкой скорости:
- Инновация по скорости → K[9..11, 3..5] ≠ 0
- K[9..11, 3..5] обычно мала (косвенная связь)
- bg оценивается за десятки секунд

Это объясняет, почему смещения датчиков оцениваются медленно — связь косвенная, а прямого измерения нет.

---

## 11. Руководство по настройке параметров

Описание, как изменение каждого параметра влияет на поведение фильтра.

### 11.1. Шум гироскопа σ_g (Q[6..8, 6..8])

Текущее значение: `σ_g = 3.39e-4 рад/√с`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×) | P[6..8,6..8] растёт быстрее → фильтр «ожидает» больших ошибок углов → **K[6..8,*] увеличивается** → коррекция по angle.dat сильнее. Но P растёт быстрее между коррекциями → при отсутствии angle.dat курс «плывёт» быстрее. |
| **Уменьшить** (2×) | P[6..8,6..8] растёт медленнее → фильтр считает углы точными → **K[6..8,*] уменьшается** → коррекция по angle.dat слабее. Курс стабильнее между коррекциями, но если реальный шум гиро велик — фильтр не компенсирует ошибки. |
| **Слишком большое** | Фильтр «не помнит» предыдущие оценки углов,每次 коррекция ~полная. Статистика шумная, результат «дёргается». |
| **Слишком малое** | Фильтр «слишком уверен» в углах, игнорирует коррекции. Накопленная ошибка не исправляется. |

**Когда увеличивать:** если гироскоп шумный (дешёвый MEMS), если нужны быстрые коррекции углов.

**Когда уменьшать:** если гироскоп точный (тактический/навигационный класс), если angle.dat отсутствует (нужна стабильность между коррекциями).

---

### 11.2. Шум акселерометра σ_a (Q[3..5, 3..5])

Текущее значение: `σ_a = 3.05e-3 м/с²/√с`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×) | P[3..5,3..5] растёт быстрее → фильтр «ожидает» больших ошибок скорости → **K[3..5,*] увеличивается** → коррекция по GPS скорости сильнее. Но P скорости растёт быстрее → скорость «шумная» между коррекциями. |
| **Уменьшить** (2×) | P[3..5,3..5] растёт медленнее → фильтр считает скорость точной → **K[3..5,*] уменьшается** → коррекция по GPS слабее. Скорость плавнее, но если акселерометр шумный — ошибки накапливаются. |
| **Слишком большое** | Скорость «дёргается» на каждом GPS обновлении. Фильтр слишком доверяет GPS. |
| **Слишком малое** | Фильтр игнорирует GPS скорости. Скорость «плывёт» по акселерометру. |

**Когда увеличивать:** при сильных вибрациях (无人机, автомобиль по бездорожью), при дешёвых акселерометрах.

**Когда уменьшать:** при стабильной платформе (корабль, аэростат), при точных акселерометрах.

---

### 11.3. Дрейф смещения гироскопа σ_bg (Q[12..14, 12..14])

Текущее значение: `σ_bg = 1.16e-5 рад/с/√с`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (10×) | P[12..14,12..14] растёт быстрее → фильтр «ожидает» что bg меняется → **K[12..14,*] увеличивается** → bg оценивается быстрее. Но оценка bg шумная. |
| **Уменьшить** (10×) | P[12..14,12..14] растёт медленнее → фильтр считает bg стабильным → **K[12..14,*] уменьшается** → bg оценивается медленно. Но оценка bg стабильнее. |
| **Слишком большое** | Оценка bg «дёргается» → коррекция гиро нестабильна → курс «прыгает». |
| **Слишком малое** | Фильтр не обновляет bg → дрейф гиро не компенсируется → курс систематически уплывает. |

**Когда увеличивать:** если bg действительно меняется (температура, вибрация), при «прогреве» датчиков.

**Когда уменьшать:** если bg стабилен (навигационный гироскоп), если нужна стабильная оценка без «прыжков».

---

### 11.4. Дрейф смещения акселерометра σ_ba (Q[9..11, 9..11])

Текущее значение: `σ_ba = 2e-5 м/с²/√с`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (10×) | ba оценивается быстрее → компенсация ускорений быстрее. Но оценка шумная → ускорения «дёргаются». |
| **Уменьшить** (10×) | ba оценивается медленно → компенсация медленная. Но оценка стабильная. |
| **Слишком большое** | ba «следит» за шумом → вычитает шум из ускорений → фильтр «не видит» реальные ускорения. |
| **Слишком малое** | ba не обновляется → систематическая ошибка ускорений → скорость «плывёт» (особенно вертикальная). |

**Когда увеличивать:** при переменных температурах (ba зависит от температуры), при сильных перегрузках.

**Когда уменьшать:** при стабильной температуре, при точных акселерометрах.

---

### 11.5. Точность GPS по координатам σ_pos (R[0..1, 0..1])

Текущее значение: `σ_pos = 5м / R_earth ≈ 7.85e-7 рад`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×, «GPS неточный») | R[0,0], R[1,1] растут → **K[0..1,*] уменьшаются** → фильтр меньше доверяет GPS координатам → координаты «плывут» по БИНС дольше. P координат растёт быстрее. |
| **Уменьшить** (2×, «GPS точный») | R[0,0], R[1,1] уменьшаются → **K[0..1,*] увеличиваются** → фильтр сильнее корректирует координаты по GPS. Координаты «прилипают» к GPS. |
| **Слишком малое** | Координаты «дёргаются» за каждым GPS измерением (включая шум GPS). Нет сглаживания. |
| **Слишком большое** | Координаты игнорируют GPS → БИНС «улетает» по инерции. |

**Когда увеличивать:** при multipath (город, каньон), при слабом сигнале GPS, при spoofing.

**Когда уменьшать:** при открытом небе (_RTK GPS), при высокоточном GPS.

---

### 11.6. Точность GPS по высоте σ_h (R[2, 2])

Текущее значение: `σ_h = 5 м`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×, 10 м) | Высота «плывёт» по БИНС дольше. Вертикальная скорость корректируется слабее (через связь). |
| **Уменьшить** (2×, 2.5 м) | Высота «прилипает» к GPS. Вертикальная скорость корректируется сильнее. |

**Особенность:** высота GPS обычно менее точна, чем горизонтальные координаты (GDOP > HDOP). Типичное значение σ_h = 1.5 · σ_pos.

---

### 11.7. Точность GPS по скорости σ_v (R[3..5, 3..5])

Текущее значение: `σ_v = 0.1 м/с`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×, 0.2 м/с) | Скорость «плывёт» по БИНС дольше. **K[3..5,*] уменьшаются** → инновация по скорости меньше влияет на состояние. |
| **Уменьшить** (2×, 0.05 м/с) | Скорость «прилипает» к GPS. **K[3..5,*] увеличиваются** → коррекция сильнее. |
| **Слишком малое** | Скорость «дёргается» за GPS шумом. Инновация по скорости шумная → оценка bg шумная. |
| **Слишком большое** | Скорость игнорирует GPS → БИНС «улетает» по инерции. |

**Когда увеличивать:** при сильных вибрациях (влияют на GPS скорость), при нестабильном GPS.

**Когда уменьшать:** при стабильном GPS, при необходимости точной оценки bg (через инновацию по скорости).

---

### 11.8. Точность angle.dat по углам σ_ang (R[6..8, 6..8])

Текущее значение: `σ_ang = 1° ≈ 0.0175 рад`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×, 2°) | Уги «плывут» по БИНС дольше. **K[6..8,*] уменьшаются** → angle.dat меньше влияет. |
| **Уменьшить** (2×, 0.5°) | Уги «прилипают» к angle.dat. **K[6..8,*] увеличиваются** → коррекция сильнее. |
| **Слишком малое** | Уги «дёргаются» за шумом angle.dat. |
| **Слишком большое** | Уги игнорируют angle.dat → БИНС «улетает» по гироскопу. |

**Когда увеличивать:** при неточном angle.dat (дешёвый датчик), при вибрациях.

**Когда уменьшать:** при точном angle.dat ( dual-antenna GPS, точный гироскоп).

---

### 11.9. Начальная неопределённость курса P₀[6,6]

Текущее значение: `P₀[6,6] = (15°)² ≈ 0.0685 рад²`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×, 30°) | Начальная K[6..6] больше → первые коррекции по angle.dat сильнее. Фильтр быстрее «забывает» начальный курс. |
| **Уменьшить** (2×, 7.5°) | Начальная K[6..6] меньше → первые коррекции слабее. Фильтр «помнит» начальный курс дольше. |
| **Слишком большое** | Начальные коррекции очень сильные → курс «прыгает» к angle.dat. |
| **Слишком малое** | Фильтр «уверен» в начальном курсе → игнорирует angle.dat → курс не корректируется. |

**Когда увеличивать:** при неуверенности в выставке (неточная выставка, сильные вибрации).

**Когда уменьшать:** при точной выставке (длинная выставка, стабильная платформа).

---

### 11.10. Начальная неопределённость скорости P₀[3..5, 3..5]

Текущее значение: `P₀[3..5,3..5] = (2.5 м/с)² = 6.25`

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×, 5 м/с) | Начальные K[3..5,*] больше → первые коррекции по GPS скорости сильнее. Скорость быстрее «прилипает» к GPS. |
| **Уменьшить** (2×, 1.25 м/с) | Начальные K[3..5,*] меньше → первые коррекции слабее. Скорость дольше «плывёт» по акселерометру. |

---

### 11.11. Шаг дискретизации T

Текущее значение: `T = 0.005 с` (200 Гц)

| Изменение | Эффект |
|-----------|--------|
| **Увеличить** (2×, 0.01 с, 100 Гц) | F-матрица: все недиагональные элементы растут пропорционально T → ошибки «распространяются» сильнее за шаг. Q-матрица: все элементы растут пропорционально T → неопределённость растёт быстрее. **Риск:** метод трапеций может стать неточным при больших T. |
| **Уменьшить** (2×, 0.0025 с, 400 Гц) | F-матрица: недиагональные элементы уменьшаются → ошибки распространяются медленнее. Q-матрица: элементы уменьшаются → неопределённость растёт медленнее. **Выигрыш:** точность интегрирования. **Цена:** больше вычислений. |

**Зависимость Q от T:** `Q ∝ T`. При увеличении T в 2× все элементы Q растут в 2×. Это компенсируется тем, что шагов меньше, но на каждый шаг неопределённость больше.

**Зависимость F от T:** `F = I + F_c · T`. При увеличении T в 2× все недиагональные элементы F растут в 2×. Это означает, что за один шаг ошибки распространяются сильнее.

---

### 11.12. Сводная таблица влияния параметров

| Параметр | ↑ Увеличить | ↓ Уменьшить |
|----------|-------------|-------------|
| σ_g (Q гиро) | Курс корректируется сильнее, но шумнее | Курс стабильнее, но коррекция слабее |
| σ_a (Q акселя) | Скорость корректируется сильнее, но шумнее | Скорость стабильнее, но «плывёт» |
| σ_bg (Q dr gyro) | bg оценивается быстрее, но шумнее | bg оценивается медленнее, но стабильнее |
| σ_ba (Q dr accel) | ba оценивается быстрее, но шумнее | ba оценивается медленнее, но стабильнее |
| σ_pos (R коорд) | Координаты «плывут» по БИНС | Координаты «прилипают» к GPS |
| σ_h (R высота) | Высота «плывёт» по БИНС | Высота «прилипает» к GPS |
| σ_v (R скорость) | Скорость «плывёт» по БИНС | Скорость «прилипает» к GPS |
| σ_ang (R углы) | Уги «плывут» по БИНС | Уги «прилипают» к angle.dat |
| P₀[6,6] (курс) | Быстрая коррекция начального курса | Медленная коррекция начального курса |
| P₀[3..5,3..5] (скорость) | Быстрая коррекция начальной скорости | Медленная коррекция начальной скорости |
| T (шаг) | Ошибки растут быстрее, Q больше | Ошибки растут медленнее, Q меньше |

---

### 11.13. Рекомендации по настройке

**Общий принцип:** Q увеличивают, когда модель неточная (шумные датчики, вибрации). Q уменьшают, когда модель точная (стабильная платформа, точные датчики). R увеличивают, когда измерения неточные (multipath, слабый сигнал). R уменьшают, когда измерения точные (открытое небо, RTK).

**Начальная настройка:**
1. Определить точность датчиков (из datasheet)
2. Задать R = (точность измерения)²
3. Задать Q = (шум модели)² · T
4. Задать P₀ = (неопределённость начальных оценок)²
5. Проверить поведение на записанных данных
6. Подстроить σ_g, σ_a, σ_bg, σ_ba по характеру ошибок

**Типичные ошибки настройки:**
- Слишком маленький Q → фильтр «не помнит» ошибки, коррекция слабая
- Слишком большой Q → фильтр «не помнит» ошибки, коррекция сильная, но шумная
- Слишком маленький R → результат «дёргается» за шумом измерений
- Слишком большой R → результат «плывёт» по модели, измерения игнорируются
