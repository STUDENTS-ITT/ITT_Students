// types.h — Базовые типы линейной алгебры.
// Матрица хранится построчно в одномерном векторе double.
// Число столбцов передаётся отдельным аргументом в функции operations.

#pragma once

#include <vector>

using Matrix = std::vector<double>;
using Vector = std::vector<double>;
